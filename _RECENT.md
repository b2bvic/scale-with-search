# Recently Modified Files

Regenerate this file with:

```bash
find "$(pwd)" -name "*.md" -type f -mmin -1440 \
  | grep -v node_modules \
  | grep -v ".obsidian" \
  | sort
```

Then group results by domain folder.

---

(Run the command above to populate this file after your first session.)
