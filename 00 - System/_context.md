# System Context

last_verified:: 2026.03.02

This is the system domain — Claude's own operational state.

## Sessions

Full session outputs stored in `Sessions/` as dated markdown files.
Cross-reference via `_log.md`.

## Patterns

Reusable solutions and recurring patterns documented in `Patterns/`.
When a pattern repeats 3+ times, it becomes a skill (`.claude/commands/`).

## Reference

Permanent factual corrections and ground truth in `Reference/`.
Corrections here override any contradicting file in the vault.
